(library (pdilemma strategy)
  (export minmax)
  (import (chezscheme)
          (atelier-kame util)
          (pdilemma util)
					(pdilemma core))
					
	(define (minmax)
	  (display "minmax strategy"))
		
)